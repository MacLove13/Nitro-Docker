{
  "name": "nitro-client-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:screenshot": "playwright test --grep @screenshot"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
